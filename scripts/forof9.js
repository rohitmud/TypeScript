let clr = ['red', 'blue', 'green'];
console.log("using simple for in");
for (let index in clr) {
    console.log(clr[index]);
}
//For of loop goes through each object in the arry.
//The same can be used to go through each letter in a string
console.log("using for of");
for (let color of clr) {
    console.log(color);
}
//# sourceMappingURL=forof9.js.map